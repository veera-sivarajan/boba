let global = 100;

fn factorial(x: i32, y: i32) {
    let apple = 10;
    let ball = 20;
    if x > y {
        let car = 30;
        println(apple + car);
    } else {
        let apple = 100;
        let ball = 250;
        println(apple + ball);
    }
}

fn main() {
    factorial(2, 1);
    factorial(1, 2);
}
